package Day40_OOP_Interface;

public interface ChildInterface extends ParentInterface {

    /**
     - If an Interface needs to inherit from another Interface,
     should use "extends" keyword!
     - For inheriting from Interface-Interface, it shouldn't override all methods!
     */
}
